from paip.pipelines.annotate_variants import AnnotateVariants



#  @pytest.fixture
#  def task(sample_task_factory):
    #  return sample_task_factory(AnnotateVariants,
                               #  extra_kwargs={''})

#  def test_run():
    # monkeypatch AnnotationPipeline.__init__ and .run_from_vcf
    # check the arguments received
